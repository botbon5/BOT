export default function dropPrivateFields(fields) {
    const optionalFields = {};
    Object.keys(fields).forEach((name) => {
        if (!fields[name].isPrivate) {
            optionalFields[name] = {
                ...fields[name],
            };
        }
    });
    return optionalFields;
}
