import validateSchema from './validateSchema.js';
export default function isSchemaValid(definition) {
    try {
        validateSchema(definition);
        return true;
    }
    catch {
        return false;
    }
}
