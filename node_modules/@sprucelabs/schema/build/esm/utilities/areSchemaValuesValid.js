import EntityFactory from '../factories/SchemaEntityFactory.js';
export default function areSchemaValuesValid(definition, values, options) {
    const instance = EntityFactory.Entity(definition, values);
    return instance.isValid(options);
}
