import { FieldDefinitions, FieldMap } from '../fields';
export default class FieldFactory {
    /** Factory for creating a new field from a definition */
    static Field<F extends FieldDefinitions>(name: string, definition: F): FieldMap[F['type']];
}
