import { buildSchema, dropFields } from '@sprucelabs/schema';
import messageBuilder from './message.builder.js';
export default buildSchema({
    id: 'sendMessage',
    fields: Object.assign({}, dropFields(messageBuilder.fields, ['id', 'dateCreated', 'source'])),
});
