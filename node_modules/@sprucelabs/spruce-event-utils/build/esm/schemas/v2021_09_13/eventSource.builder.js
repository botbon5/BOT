import { buildSchema } from '@sprucelabs/schema';
import eventTargetBuilder from './eventTarget.builder.js';
export default buildSchema({
    id: 'eventSource',
    name: 'event source',
    moduleToImportFromWhenRemote: '@sprucelabs/spruce-event-utils',
    importsWhenRemote: ["import '@sprucelabs/spruce-event-utils'"],
    fields: Object.assign(Object.assign({}, eventTargetBuilder.fields), { proxyToken: {
            type: 'id',
            label: 'Proxy token',
        } }),
});
