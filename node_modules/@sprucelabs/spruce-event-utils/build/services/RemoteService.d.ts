import { EnvService } from '@sprucelabs/spruce-skill-utils';
import { Remote } from '../constants';
export default class RemoteService {
    private env;
    constructor(env: EnvService);
    set(remote: Remote): void;
    getHost(): import("@sprucelabs/spruce-skill-utils").EnvValue | undefined;
    getRemote(): Remote | null;
}
