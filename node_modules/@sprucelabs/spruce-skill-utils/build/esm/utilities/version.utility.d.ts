export declare function formatDate(date: Date): string;
declare const versionUtil: {
    getAllVersions(dirToRead: string): {
        intValue: number;
        constValue: string;
        dirValue: string;
    }[];
    generateVersion(dateFormattedString?: string): {
        intValue: number;
        constValue: string;
        dirValue: string;
    };
    latestVersionAtPath(path: string): {
        intValue: number;
        constValue: string;
        dirValue: string;
    };
    extractVersion(cwd: string, path: string): {
        intValue: number;
        constValue: string;
        dirValue: string;
    };
    resolvePath(cwd: string, ...paths: string[]): string;
    resolveNewLatestPath(cwd: string, ...paths: string[]): string;
    isValidVersion(version: string): boolean;
    assertValidVersion(version: string): void;
};
export default versionUtil;
