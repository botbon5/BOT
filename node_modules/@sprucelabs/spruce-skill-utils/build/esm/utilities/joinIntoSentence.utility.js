export default function joinIntoSentence(words) {
    const last = words.pop();
    if (words.length === 0) {
        return last !== null && last !== void 0 ? last : '';
    }
    return words.join(', ') + ' & ' + last;
}
