declare const namesUtil: {
    /** Gets you a name good for using in an import statement based off a file path */
    toFileNameWithoutExtension(filePath: string): string;
    /** First name => FirstName */
    toCamel(name: string): string;
    /** First name => FirstName */
    toPascal(name: string): Capitalize<string>;
    /** First name => FIRST_NAME */
    toConst(name: string): string;
    /** First name => First names */
    toPlural(name: string): string;
    /** First name => First name */
    toSingular(name: string): string;
    /** First name => first-name */
    toKebab(name: string): string;
    /** First name => first_name */
    toSnake(name: string): string;
};
export default namesUtil;
