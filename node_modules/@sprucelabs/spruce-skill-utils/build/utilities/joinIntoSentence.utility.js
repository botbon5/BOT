"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = joinIntoSentence;
function joinIntoSentence(words) {
    const last = words.pop();
    if (words.length === 0) {
        return last ?? '';
    }
    return words.join(', ') + ' & ' + last;
}
