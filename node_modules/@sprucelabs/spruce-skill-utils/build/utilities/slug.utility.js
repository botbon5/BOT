"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = slug;
function slug(slug) {
    return `${slug
        .toLowerCase()
        .replace(/[^a-z0-9]/gi, '-')
        .replace(/--*/, '-')
        .trim()}`;
}
