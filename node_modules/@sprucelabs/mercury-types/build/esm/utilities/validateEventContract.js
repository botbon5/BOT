import { validateSchemaValues } from '@sprucelabs/schema';
import eventContractSchema from './../.spruce/schemas/mercury/v2020_12_25/eventContract.schema.js';
import SpruceError from '../errors/SpruceError.js';
export default function validateEventContract(contract) {
    try {
        validateSchemaValues(eventContractSchema, contract);
    }
    catch (err) {
        throw new SpruceError({
            code: 'INVALID_EVENT_CONTRACT',
            originalError: err,
        });
    }
}
