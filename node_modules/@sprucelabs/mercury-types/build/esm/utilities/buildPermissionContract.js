import { validateSchemaValues } from '@sprucelabs/schema';
import permissionContractSchema from './../.spruce/schemas/mercury/v2020_12_25/permissionContract.schema.js';
export default function buildPermissionContract(contract) {
    validateSchemaValues(permissionContractSchema, contract);
    return contract;
}
