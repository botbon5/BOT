import { Connection, PublicKey } from "@solana/web3.js";
import { Instruction } from "@orca-so/sdk/dist/public/utils";
import { TransactionPayload } from "@orca-so/sdk/dist/public/utils/models";
import { Owner } from "@orca-so/sdk/dist/public/utils/web3/key-utils";
export declare class TransactionBuilder {
    private connection;
    private feePayer;
    private instructions;
    private owner;
    constructor(connection: Connection, feePayer: PublicKey, owner: Owner);
    addInstruction(instruction: Instruction): TransactionBuilder;
    build(): Promise<TransactionPayload>;
}
