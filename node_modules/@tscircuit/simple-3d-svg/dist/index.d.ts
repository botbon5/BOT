interface Point3 {
    x: number;
    y: number;
    z: number;
}
type RGBA = [number, number, number, number];
type Color = RGBA | string;
interface Box {
    center: Point3;
    size: Point3;
    color: Color;
    rotation?: Point3;
    topLabel?: string;
    topLabelColor?: Color;
}
interface Camera {
    position: Point3;
    lookAt: Point3;
    focalLength?: number;
}
interface Scene {
    boxes: Box[];
    camera: Camera;
}
declare function renderScene(scene: Scene, opt?: {
    width?: number;
    height?: number;
    backgroundColor?: Color;
}): string;

export { type Box, type Camera, type Color, type Point3, type RGBA, type Scene, renderScene };
